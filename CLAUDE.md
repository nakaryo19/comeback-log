# CLAUDE.md

このファイルは、Claude Code がこのリポジトリで作業する際のガイダンスです。

## プロジェクト概要

**comeback-log（挽回ログ）** は、目標達成までのタスク管理と感情ログを紐づけて記録し、
タスクの実績と感情の波の相関を可視化することで「自分自身の攻略法」に気づけるアプリ。

- ターゲット：浪人・再受験・キャリア再構築など「一度うまくいかなかったことに、もう一度向き合っている人」
- 差別化ポイント：タスク実績と感情ログを同じ時間軸で可視化する分析力（既存の目標管理/習慣化アプリとの違い）
- 詳細な要件定義は `docs/要件定義書_v0.2.md` を参照。実装判断に迷ったら、まずこのドキュメントを確認すること。
- ブランチ運用は `docs/branch/ブランチ戦略.md` を参照。ブランチを作成する際は、必ずこのドキュメントの命名規則・ワークフローに従うこと。

## 開発方針・制約（重要）

- **AI APIを一切使用しない。** 当初はAIコーチング機能を検討したが、無料運営とプライバシー保護を優先し撤廃した。
  「気づきをAIが与える」のではなく「気づきをユーザー自身に委ねる」可視化中心の設計に統一している。
  相関分析やハイライト表示は、すべて**ルールベース（統計処理・条件分岐＋固定テンプレート文）**で実装すること。
  外部LLM APIの呼び出しをコードに追加してはならない。
- **完全無料運営が前提。** Expo（無料枠）＋ Supabase（無料枠）のみで完結する構成を維持する。
  新しい依存サービスを追加する際は、無料枠の範囲で収まるか必ず確認する。
- **感情ログのプライバシーを最優先する。** `EmotionLog`（スコア・タグ・自由記述）は、いかなる場合も
  外部API送信・公開プロフィールへの露出をしてはならない。公開してよいデータ（達成率・継続日数などのサマリー）
  とは、DB設計・アクセス権限レベルで構造的に分離すること。
- **不調検出の表現トーンに注意する。** 週次ハイライトで「不調週」を検出する際、評価的な言葉
  （「不調」「低調」等）やアラート色（赤・強い黄）は使わない。労い・観察を促すトーンの文言と
  落ち着いた配色に統一する（詳細は要件定義書 4-3 参照）。

## 技術スタック

| レイヤー | 技術 |
|---|---|
| フロントエンド | React Native (Expo) + react-native-web（Web/iOS/Androidを単一コードベースでカバー） |
| バックエンド/DB | Supabase（PostgreSQL + Auth + Storage） |
| グラフ描画 | クライアント側の軽量チャートライブラリ（Chart.js系 or Victory Native等、選定は実装時に確定） |
| 相関分析・集計 | クライアント側 or Supabase Edge Functionsでのルールベース処理（外部AI API不使用） |

## よく使うコマンド

```bash
# 開発サーバー起動
npm run start

# Web版の起動
npm run web

# Lint（expo lint / ESLint flat config）
npm run lint

# 型チェック（tsc --noEmit）
npm run typecheck

# テスト（jest-expo + React Native Testing Library）
npm test

# テスト（ウォッチモード）
npm run test:watch
```

## ディレクトリ構成（予定）

```
comeback-log/
├── docs/
│   ├── 要件定義書_v0.2.md     # 要件定義の正本。仕様判断はここを参照
│   └── branch/
│       └── ブランチ戦略.md    # ブランチ運用の正本。ブランチ作成時はここを参照
├── app/                      # 画面（Expo Router想定）
│   ├── (home)/                # ホーム画面：今日のタスク＋感情記録の導線
│   ├── goals/                 # 目標管理画面（ゴールツリー）
│   ├── dashboard/              # 可視化ダッシュボード（層1：ホーム / 層2：詳細分析）
│   └── profile/                # 公開プロフィール設定・閲覧
├── components/                # 共通UIコンポーネント
├── lib/
│   ├── supabase/               # Supabaseクライアント・クエリ
│   └── insights/                # ハイライト判定・相関分析のルールベースロジック（AI不使用）
└── types/                     # 型定義（Goal / SubGoal / Task / EmotionLog / PublicProfile 等）
```

## データモデル（要件定義書より抜粋）

```
User
 └─ Goal（大目標）
     └─ SubGoal（中目標・初回登録時は自動生成される「仮の中目標」を含む）
         └─ Task（日次タスク：status, date）

EmotionLog
 - task_id（タスクと1:1で紐付け）
 - score（1-5、必須）
 - tag（1つ、任意）
 - free_text（任意）

PublicProfile（任意公開・デフォルト非公開）
 - goal_summary / achievement_rate / streak_days / is_public
 - EmotionLog・free_text へは参照不可（構造的に分離）
```

## 開発フェーズ（実装の優先順位）

1. **Phase 1（現在着手中）**：タスク管理（ゴールツリー・チェックイン）＋感情ログ（タスク1:1紐付け、
   スコア→タグ→自由記述の2段階入力）＋ホーム画面の最小限の数値表示のみ。グラフや詳細分析は含めない。
   まずは開発者本人が乗り換えて使い続けられることを検証する。
2. **Phase 2**：2層構造の可視化ダッシュボード（ホーム＝直近7日の相関散布図＋週次サマリー、
   詳細分析＝カレンダーヒートマップ・週次サマリーリスト・タグ推移グラフ）とハイライト表示
   （好調週/不調週判定、詰め込み・好循環パターン検出）を実装。Phase 1で数週間分のデータが
   蓄積されてから着手する。
3. **Phase 3**：公開プロフィール機能とソーシャル要素（他人の公開プロフィール閲覧）。

新機能の実装依頼を受けたら、まずこのフェーズと矛盾しないか（フェーズを先取りしすぎていないか）を確認すること。

## コーディング規約

- ESLint を導入済み（`eslint.config.js`、`eslint-config-expo` ベースの flat config）。コミット前に `npm run lint` と `npm run typecheck` を通すこと。
- `react-hooks/set-state-in-effect` は既存のマウント時データ取得パターンを許容するため warn に下げている。新規コードでは極力このパターンを増やさないこと。
- CI（GitHub Actions: `.github/workflows/ci.yml`）が `main` への push / PR で Lint・型チェック・テストを実行する。
- テストは jest-expo + React Native Testing Library（v14、`render`/`fireEvent` は async）。テストファイルは
  対象の隣の `__tests__/` に `*-test.ts(x)` として置く。Supabase に依存するモジュールはテストで
  `jest.mock` すること（実クライアントは環境変数必須のため、テストから import してはならない）。
- フォーマッタ（Prettier）は未導入。導入し次第このセクションを更新すること。
