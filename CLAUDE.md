# CLAUDE.md

このファイルは、Claude Code がこのリポジトリで作業する際のガイダンスです。

## プロジェクト概要

**comeback-log（挽回ログ）** は、目標達成までのタスク管理と感情ログを紐づけて記録し、
タスクの実績と感情の波の相関を可視化することで「自分自身の攻略法」に気づけるアプリ。

- ターゲット：浪人・再受験・キャリア再構築など「一度うまくいかなかったことに、もう一度向き合っている人」
- 差別化ポイント：タスク実績と感情ログを同じ時間軸で可視化する分析力（既存の目標管理/習慣化アプリとの違い）
- 詳細な要件定義は `docs/要件定義書_v0.2.md` を参照。実装判断に迷ったら、まずこのドキュメントを確認すること。
- ブランチ運用は `docs/branch/ブランチ戦略.md` を参照。ブランチを作成する際は、必ずこのドキュメントの命名規則・ワークフローに従うこと。

## 開発方針・制約（重要）

- **AI APIを一切使用しない。** 当初はAIコーチング機能を検討したが、無料運営とプライバシー保護を優先し撤廃した。
  「気づきをAIが与える」のではなく「気づきをユーザー自身に委ねる」可視化中心の設計に統一している。
  相関分析やハイライト表示は、すべて**ルールベース（統計処理・条件分岐＋固定テンプレート文）**で実装すること。
  外部LLM APIの呼び出しをコードに追加してはならない。
- **完全無料運営が前提。** Expo（無料枠）＋ Supabase（無料枠）のみで完結する構成を維持する。
  新しい依存サービスを追加する際は、無料枠の範囲で収まるか必ず確認する。
- **Supabase の自動一時停止に注意する。** 無料プランは7日間DB活動がないとプロジェクトが一時停止される。
  対策として `.github/workflows/keep-alive.yml` が毎日 ping を送っている（詳細は
  `docs/運用/Supabase自動停止対策.md` を参照）。DBに関わる構成を変更する際は、この ping が
  壊れていないか確認すること。
- **感情ログのプライバシーを最優先する。** `EmotionLog`（スコア・タグ・自由記述）は、いかなる場合も
  外部API送信・公開プロフィールへの露出をしてはならない。公開してよいデータ（達成率・継続日数などのサマリー）
  とは、DB設計・アクセス権限レベルで構造的に分離すること。
- **不調検出の表現トーンに注意する。** 週次ハイライトで「不調週」を検出する際、評価的な言葉
  （「不調」「低調」等）やアラート色（赤・強い黄）は使わない。労い・観察を促すトーンの文言と
  落ち着いた配色に統一する（詳細は要件定義書 4-3 参照）。

## 技術スタック

| レイヤー | 技術 |
|---|---|
| フロントエンド | React Native (Expo) + react-native-web（Web/iOS/Androidを単一コードベースでカバー） |
| バックエンド/DB | Supabase（PostgreSQL + Auth + Storage） |
| グラフ描画 | **ライブラリ不使用。** `View` と `StyleSheet` だけで描いている（`react-native-svg` も未導入） |
| 相関分析・集計 | クライアント側 or Supabase Edge Functionsでのルールベース処理（外部AI API不使用） |

**グラフにチャートライブラリを入れないこと。** 散布図・ヒートマップ・棒グラフはいずれも
`components/home/DailyTrendChart.tsx` や `components/analytics/` のように、
`View` の寸法と背景色だけで組んである。必要な表現がその範囲で足りているうちは、
バンドルサイズと依存の更新コストを増やさない判断を維持する。
新しいグラフを足すときも既存の描き方に倣うこと。

## よく使うコマンド

```bash
# 開発サーバー起動
npm run start

# Web版の起動
npm run web

# Lint（expo lint / ESLint flat config）
npm run lint

# 型チェック（tsc --noEmit）
npm run typecheck

# テスト（jest-expo + React Native Testing Library）
npm test

# テスト（ウォッチモード）
npm run test:watch

# 法務ページとLPのビルド（dist-legal/ に出力。Cloudflare Pages で公開）
# プレースホルダの実値が要る。docs/legal/values.local.json（Git管理外）か
# 環境変数 LEGAL_VALUES を用意しないと失敗する
npm run build:legal

# 依存ライブラリのライセンス一覧を再生成（依存を足したら必ず実行）
npm run build:licenses
```

## ディレクトリ構成

**Expo Router は使っていない。** `app/` ディレクトリは存在せず、画面の切り替えは
`components/MainApp.tsx` の `view` state による分岐で行っている。画面を足すときも
この分岐に足すこと。ルーターを導入したくなったら、まず本当に必要か検討する
（現状は画面数が少なく、URL を持つ必要があるのは Web の法務ページだけで、
そちらは別ビルド `dist-legal/` として静的生成している）。

```
comeback-log/
├── App.tsx                    # 入口。ErrorBoundary → AuthProvider → MainApp/AuthScreen
├── docs/
│   ├── 要件定義書_v0.2.md      # 要件定義の正本。仕様判断はここを参照
│   ├── branch/ブランチ戦略.md   # ブランチ運用の正本。ブランチ作成時はここを参照
│   ├── legal/                 # 利用規約・プライバシーポリシー（コミットする）
│   └── 運用/                   # 運用メモ。**大半は Git 管理外**（後述）
├── components/
│   ├── MainApp.tsx            # ログイン後の画面切り替えとハンバーガーメニュー
│   ├── auth/                  # ログイン・パスワード再設定
│   ├── goals/                 # 目標管理（ゴールツリー）・初回オンボーディング
│   ├── home/                  # 今日のタスク＋感情記録の導線、直近7日の散布図
│   ├── analytics/             # 詳細分析（ヒートマップ・週次サマリー・タグ推移）
│   └── settings/              # アカウント削除・データエクスポート・このアプリについて
├── lib/
│   ├── supabase/              # クライアント・クエリ・認証コンテキスト・エラー文言
│   ├── insights/              # ハイライト判定・集計のルールベースロジック（AI不使用）
│   ├── export/                # データエクスポート（CSV 組み立て・保存）
│   └── theme.ts               # 配色・余白・角丸の定義。画面から直接色を書かない
├── scripts/                   # LP/法務ページ生成、ライセンス一覧、バックアップ、ダミーデータ投入
├── supabase/
│   ├── migrations/            # スキーマと RLS
│   ├── functions/             # Edge Functions（アカウント削除。service_role はここだけ）
│   └── email-templates/       # 認証メールの文面（コミットする）
└── types/database.ts          # Supabase から生成した DB 型
```

## データモデル（要件定義書より抜粋）

```
User
 └─ Goal（大目標・複数可。並行する目標を分けて管理できる）
      - achieved_at（達成日時・null は未達成。手動でのみ設定し、タスクの完了状況とは連動しない）
     └─ SubGoal（中目標・初回登録時や大目標の追加時に自動生成される「仮の中目標」を含む）
          - achieved_at（達成日時・Goal と同じ扱い）
         └─ Task（日次タスク：status, date）

EmotionLog
 - task_id（タスクと1:1で紐付け）
 - score（1-5、必須）
 - tag（1つ、任意）
 - free_text（任意）

PublicProfile（任意公開・デフォルト非公開）
 - goal_summary / achievement_rate / streak_days / is_public
 - EmotionLog・free_text へは参照不可（構造的に分離）
```

## 開発フェーズ（実装の優先順位）

1. **Phase 1（完了）**：タスク管理（ゴールツリー・チェックイン）＋感情ログ（タスク1:1紐付け、
   スコア→タグ→自由記述の2段階入力）＋ホーム画面の数値表示。
2. **Phase 2（完了）**：2層構造の可視化ダッシュボード（ホーム＝直近7日の相関散布図＋週次サマリー、
   詳細分析＝カレンダーヒートマップ・週次サマリーリスト・タグ推移グラフ）とハイライト表示
   （好調週/不調週判定、詰め込み・好循環パターン検出）。
3. **Phase 3（保留）**：公開プロフィール機能とソーシャル要素（他人の公開プロフィール閲覧）。
   **インストール数が伸びて実用性が確認できてから判断する**という方針で止めている。
   PublicProfile はデータモデルにはあるが未実装なので、**LP やストア掲載文で触れないこと。**

**2026-09-05 に iOS 版を App Store でリリースした**（`1.0 (5)`、アプリID `6805856355`、
日本のみ配信・無料）。以後は Phase 3 に限らず、**利用者が既にいる前提で変更を考えること。**
とくに以下は、リリース前と扱いが変わっている。

- **DB のスキーマ変更・RLS 変更は、既存データを壊さない形にする。** マイグレーションは
  追加方向を基本とし、削除・型変更は移行手順とセットで考える。
- **画面の作り替えは、既に記録を持っている利用者から見て破壊的でないかを確認する。**
- **ストアに出すには審査が要る。** 提出のたびに審査用デモアカウントへダミーデータを
  入れ直す必要がある（空だと「機能が動作しない」と判定される）。手順は Git 管理外の
  `docs/運用/審査提出メモ.md` と `docs/運用/リリース計画.md` を参照。

新機能の実装依頼を受けたら、まずこのフェーズと矛盾しないか（フェーズを先取りしすぎていないか）を確認すること。

## コーディング規約

- ESLint を導入済み（`eslint.config.js`、`eslint-config-expo` ベースの flat config）。コミット前に `npm run lint` と `npm run typecheck` を通すこと。
- `react-hooks/set-state-in-effect` は既存のマウント時データ取得パターンを許容するため warn に下げている。新規コードでは極力このパターンを増やさないこと。
- CI（GitHub Actions: `.github/workflows/ci.yml`）が `main` への push / PR で Lint・型チェック・テストを実行する。
- テストは jest-expo + React Native Testing Library（v14、`render`/`fireEvent` は async）。テストファイルは
  対象の隣の `__tests__/` に `*-test.ts(x)` として置く。Supabase に依存するモジュールはテストで
  `jest.mock` すること（実クライアントは環境変数必須のため、テストから import してはならない）。
- **フォーマッタ（Prettier）は導入しない方針。** ESLint と型チェックで十分に回っており、
  一人開発で書式の衝突が起きないため。導入すると全ファイルに差分が走り、
  以後の `git blame` が読みにくくなる代償のほうが大きい。
  「未導入だから入れる」という判断はしないこと。

## Git 管理外のファイルに注意する

このリポジトリは公開されている。`docs/運用/` の**大半と `docs/発信/` はローカル専用メモ**で、
`.gitignore` ではなく **`.git/info/exclude`** で除外している（リポジトリを見ても除外設定が
見えないので、存在に気づきにくい）。運用の実値・収益化の検討・審査の応答文面などが入っている。

- **`git add -A` と `git add .` を使わない。** 管理外メモを巻き込む事故を実際に起こしている。
  必ず `git status --short` で確認してから、変更ファイルを明示して `git add <path>` する。
- 除外されているか怪しいときは `git check-ignore -q <path>` で確かめる。
- `docs/運用/` でも `Supabase自動停止対策.md` / `エラーログの運用.md` / `バックアップ運用.md`
  の3件は**追跡している**。`docs/legal/` と `supabase/email-templates/` も公開前提でコミットする。

**秘匿値をチャットに貼らない・ファイルに書かない。** とくに次のものは扱いを間違えると影響が大きい。

- **`service_role` キーは全 RLS を迂回する。** Edge Function の環境変数にだけ置き、
  `.env.local` にも GitHub Secrets にもリポジトリにも置かない。
- `SUPABASE_ACCESS_TOKEN` / `BACKUP_PASSPHRASE` / DB パスワード / Expo のアクセストークン /
  Brevo の SMTP キー（利用者名義でメールを送れる）/ 審査用デモアカウントのパスワード。
- パスワード再設定リンクの URL には**有効なセッショントークンが入っている**。
- `EXPO_PUBLIC_SUPABASE_ANON_KEY` は RLS で守る前提の公開鍵なので、これだけは例外。
